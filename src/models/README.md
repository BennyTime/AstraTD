# Models

This folder contains all procedural 3D model builders used in Astra TD.

## Structure
- `board/` - Arena platform, borders, spawn structure, floating props.
- `enemies/` - Enemy archetype model builders.
- `misc/` - Shared environment/special models (for example, Nexus).
- `towers/` - Tower model builders (Laser, Pulse).